# [Nudge] 2026-04-14 — 디자인 시스템 & 아이콘 컨셉 4종, Phase 0 클로즈

> 작업 시간: 22:10 ~ 23:05
> 분류: #기획 #디자인 #Phase0마감

## 📌 오늘 한 일

- **DESIGN.md 신규 작성** — Nudge 전용 디자인 기준 단일 소스. 디자인 원칙 5개, Teal 팔레트(teal/50 ~ teal/700 + 뉴트럴), 다크모드 맵핑, 타이포 스케일, 코너·간격·그림자 토큰, 아이콘 컨셉 4종 요약 + 추천 순위, 위젯 비주얼 초안까지 한 파일로 정리.
- **아이콘 컨셉 4종 SVG 생성** (`nudge/icon-concepts/`)
  - **A — Ripple Tap**: 중앙 점 + 두 겹 얇은 링. 탭이 번지는 철학 시각화. → **1순위 가결정**
  - **B — +1 Badge**: "+1" 타이포 (SF Rounded 대체 스택). 스토어 전환율 백업안
  - **C — Single Notch**: tally 한 획. 극단 미니멀, 작은 사이즈 리스크
  - **D — Fingertip Dot**: 여백 미학, 누르고 싶은 유도. 존재감 약함
- **preview.html 생성** — 4개 아이콘을 대·중·소 3사이즈로 동시 렌더링(홈스크린/스포트라이트/세팅앱 시뮬레이션) + 팔레트 스와치 바.
- **PROGRESS.md 갱신** — Phase 0 마지막 체크박스 `[x]`로 전환, 완료 작업 표에 오늘 줄 추가, 관련 파일 표에 `DESIGN.md` · `icon-concepts/` 경로 추가.
- **masterboard 연동** — `project-data.json`의 `n3` 태스크를 `done: true`로 변경하고 텍스트에 산출물 명시. schedule 배열에 2026-04-14 Nudge 디자인 완료 라인 추가. `project-data.js`는 JSON으로부터 `node`로 재생성.

## 🤔 결정 사항

- **맥스아웃과 컬러 정반대** — 맥스아웃이 주황(#7c2d12, 따뜻·각성)이면 Nudge는 티얼(#14b8a6, 차가움·평온)로 고정. 포트폴리오에 나란히 놓았을 때 "한 개발자가 정반대 페르소나 두 개를 커버한다"가 한눈에 읽히도록 한 의도적 선택.
- **SF Pro Rounded 채택** — 본문·숫자 모두 Rounded 웨이트. 날카로운 서체는 "부담 낮춤" 어조와 충돌하니까. 위젯 숫자가 이 앱의 주인공이므로 Rounded 선택이 정체성을 결정.
- **A안(Ripple Tap)을 가결정** — 4개 중 철학 표현력과 스토어 인지도를 모두 통과한 유일한 안. B안은 A안이 실기 테스트에서 약하면 갈아끼울 보험으로 보관. C·D는 마케팅 이미지용으로만 가능성 열어둠.
- **아이콘 1024×1024 플랫으로 생산, 프리뷰에서만 `rx=200`** — iOS가 앱 아이콘 모서리를 알아서 마스킹하니까 원본은 모서리 없어야 한다. 프리뷰 HTML은 사람 눈에 자연스럽게 보이도록 rx 적용.

## 🐛 문제와 해결 과정

- SVG에서 SF Pro Rounded 직접 렌더링은 불가. B안 "+1 Badge"는 `font-family` 스택에 `"-apple-system, SF Pro Rounded, system-ui"`를 걸어서 **macOS/iOS에서 열 때만** 진짜 SF Rounded가 나오도록 처리. 다른 OS에서 보면 폰트가 달라질 수 있음 — PNG 익스포트 시점에 주의 필요.

## 🔍 검증 결과 (로컬)

- `PROGRESS.md` Phase 0 체크박스 전환 확인 ✅
- `project-data.json` n3 done=true 확인 ✅
- `project-data.js` 재생성 후 문자열 매칭(`grep '"n3"'`, `grep 'Nudge] 디자인'`) 모두 일치 ✅
- 배포/실기 검증: 아직 없음 (Phase 1 Xcode 진입 시 아이콘 PNG 익스포트 → 홈스크린 실제 배치 테스트 예정)

## ➕ 세션 연장분 — 화면 시안 4화면

- A안 확정 후, 코드 작업 진입 전에 시안을 먼저 보기로 결정. "Nudge처럼 미니멀이 곧 정체성인 앱에선 여백/타이포/숫자 크기를 코드 안에서 반복 조정하는 것보다 HTML 시안에서 먼저 합의하는 쪽이 빠르다"는 판단.
- `mockups/screens.html` 생성. iPhone 15 비율(320×690) 약식 프레임 4개 그리드:
  1. **메인 카운터** — 상단 운동 pill + 통계 버튼, 중앙 128pt 대형 숫자 + "+1" 델타, 하단 원형 +1 버튼 (120pt) + undo 텍스트
  2. **운동 선택** — 3행 리스트(푸시업/풀업/스쿼트), 활성 행은 teal/50 배경 + 체크, 행마다 오늘·이번 주 수치 미세 표기
  3. **일간 통계** — TODAY TOTAL 72pt 숫자 + 운동별 3행 + dashed 박스에 평생 누적
  4. **주간 통계** — 세그먼트(일/주/월/년) + 7일 스택 바(teal 600/500/300) + 주간 합계 + 지난 주 대비 델타
- 풋터에 현태님 검토 체크리스트 4문항 포함 (숫자 크기, 비교 유발 여부, dashed 박스 톤, 스택 바 색 구분).

## 📅 다음에 할 일

- **시안 검토 → 수정 루프 (다음 세션 초반).**
  1. `mockups/screens.html`을 브라우저로 열어 4화면 검토
  2. 숫자 크기·여백·운동 선택 행 노출 정도 피드백 → 수정
- **Phase 1 진입 준비:**
  3. A안 아이콘 1024×1024 PNG 익스포트 후 `icon-concepts/A-ripple-tap.png` 저장
  4. Xcode 프로젝트 셋업 (iOS 앱 + Widget Extension + watchOS 타겟, Bundle ID 규칙)
  5. App Group + SwiftData + Interactive Widget 최소 프로토타입 (기술 리스크 3종 선검증)
- 위젯 시안은 HTML로 더 그리지 않고 Phase 1 후반 실기 프로토타입에서 바로 검증 예정 (정적 시안의 한계가 있어 시간 효율이 낮음).

## ➕ 세션 연장분 2 — 시안 확정 · 아이콘 SVG 전환 · 수익화 로드맵

- **시안 확정**: 현태님 피드백으로 운동 아이콘만 수정 결정. 4화면 레이아웃·여백·타이포는 그대로 통과.
- **emoji → SVG 라인아트 교체**: 🫲/🤸/🦵 → 💪/🧗/🏋️ 단계를 거쳐 최종적으로 모두 **커스텀 SVG 라인아트**로 전환. 이유: emoji는 OS/폰트 버전마다 모양이 달라 디자인 시스템 토큰과 분리되는 문제. Teal/600 스트로크·rounded linecap·48 viewBox 공통 규격으로 3개 아이콘 통일. 운동 선택 행(30px)·일간 통계 행(20px) 모두 자동 스케일되도록 CSS 분기.
- **수익화 로드맵 `MONETIZATION.md` 신규 작성**: Nudge의 3가지 구조적 제약(위젯 광고 금지·짧은 세션·미니멀 정체성) 분석 → Freemium 하이브리드 결정. Phase A(v1.0 무광고) → Phase B(v1.1 IAP $2.99 "개발자 응원하기") → Phase C(v1.2 통계 화면 interstitial + IAP 재사용)의 3단계 로드맵. 광고 배치 허용/금지 표, AdMob/StoreKit2 구현 체크리스트, 수익 기대치 현실 체크(Nudge만으론 생활비 안 나옴 — 맥스아웃과의 포트폴리오 조합이 본질적 가치)까지 포함.
- **masterboard 반영**: `project-data.json`에 태스크 n13~n15 추가(수익화 3단계), schedule에 2026-04-14 시안 확정·MONETIZATION.md 작성 라인 추가. `project-data.js` 재생성.

## 🤔 결정 사항 (연장분 2)

- **emoji 대신 SVG를 디자인 시스템 토큰으로 격상**. OS 간 일관성 + 정체성 관리 + 스케일 유연성 3가지 이유로 장기적으로 더 유리. 추가 에셋 3개 유지 비용은 감수.
- **v1.0 출시는 광고·IAP 모두 없음으로 확정**. 초기 리뷰/별점이 앱 스토어 알고리즘상 가장 중요한 시기에 광고를 넣는 리스크 > 초기 광고 수익(거의 0). 장기 ROI 기준 결정.
- **광고는 앱 본체 "통계 화면 진입 시점"에만 interstitial 1회 허용**. 위젯/메인 카운터/운동 선택 화면은 영구 금지. 이 구조를 MONETIZATION.md에 계약처럼 못박아 나중에 흔들리지 않게 문서화.
- **v1.0 코드에 `@AppStorage("adsRemoved")` 플래그는 미리 심되 항상 true 고정**. v1.2 진입 시 플래그 값만 열면 광고 분기 활성. Phase 1 설계 때 구현의 "후크" 지점 잊지 말 것.

## 🔍 검증 결과 (연장분 2)

- `mockups/screens.html`에서 emoji 0개 / SVG 6개(3 운동 × 2 화면) 확인 ✅
- `.s2 .row .ic svg`·`.s3 .bd-row .ic svg` CSS 규칙 추가 확인 ✅  
- `MONETIZATION.md` 생성 확인 ✅
- `project-data.json` 태스크 수 68개·schedule 28개로 증가 확인 ✅
- 로컬/배포 검증은 git push & 배포 후 별도 수행 예정.

## 💭 느낀 점

- "아이콘을 그린다"가 "철학을 한 번 더 검증한다"와 같은 일이 된다는 걸 다시 느낌. Ripple Tap을 그리고 보니 앱 설명 한 줄이 그림으로 요약되더라. **아이콘이 설명을 대신 못 하면 앱 콘셉트 자체가 복잡한 거라는 테스트가 됨**.
- 맥스아웃/Nudge의 컬러 대비를 정리하면서, 이 둘이 하나의 포트폴리오 자산으로 더 강해진다는 점이 보였다. 비슷한 앱 두 개가 아니라 "정반대를 노려서 둘 다"라는 서사가 가능해졌다.
- Phase 0를 이틀 내에 "기획 + 구조 + 디자인 시스템"까지 묶어서 닫는 건 일반적 페이스보다 빠른 편. 대신 Phase 1에선 기술 리스크 검증을 빠르게 돌려야 일정이 밀리지 않는다.
